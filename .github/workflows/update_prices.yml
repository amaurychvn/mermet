name: Mise à jour Prix Pétrole

on:
  schedule:
    - cron: '30 6 * * *' # Tous les jours à ~7h30/8h30 en France
  workflow_dispatch: # Permet de lancer manuellement pour tester

# Ces permissions permettent au bot d'écrire dans ton dépôt privé
permissions:
  contents: write

jobs:
  update-data:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run scraping script
        run: python main.py

      - name: Commit and push changes
        run: |
          git config --global user.name 'GitHub Actions Bot'
          git config --global user.email 'actions@github.com'
          # Vérifie s'il y a des modifs avant de commit pour éviter une erreur
          if [[ -n $(git status -s) ]]; then
            git add prix_carburants_quotidien.csv
            git commit -m "Auto-update: Prix carburants du jour"
            git push
          else
            print("Aucun changement détecté aujourd'hui.")
          fi
